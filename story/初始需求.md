# 明道云预约看板视图插件 开发需求
## 【强制目录权限规则（最高优先级，必须严格遵守）】
1. 本次仅授权 D:\AI\项目源码\明道云插件开发\预约看板\预约看板新版（视图展示解耦）\ 这一个目录，为唯一可读写目录。
2. 禁止读取、编辑、新增、删除、引用该目录以外的所有文件、目录、代码及配置。
3. 禁止跨目录调用、导入外部代码，禁止访问项目根目录与其他业务模块。
4. 所有新增文件、代码、文档、函数，仅允许存放在授权目录内。
5. 任何文件改动、新建操作，必须先标注完整文件路径，等待我确认后再执行。
6. 仅使用我主动提供的该目录内参考代码，不得主动检索其他目录文件。

## 一、项目背景
本项目为明道云视图插件（预约时间看板），目前基础功能已开发完成，拥有完整业务逻辑、历史代码与成熟取数流程。
过往开发中，在明道云环境变量调用、字段解析、数据取数、查询入参格式、筛选条件配置等环节频繁踩坑；现有相关Skill规则描述不完整，导致开发过程中需要反复猜测、多次试错，还额外增加了大量异常兜底逻辑，同时偶尔出现数据错误，开发效率较低。

本次改造不改动原有业务逻辑、页面UI、交互效果与整体功能流程，全程适配明道云原生接口、环境变量及平台运行规则。

## 二、核心代码约束
1. 原有业务逻辑、页面样式、交互逻辑、数据结果完全保留，**禁止修改原有业务核心代码**。
2. 本次仅做架构分层、规则标准化、通用函数封装、代码重构整合，不新增、删减原有业务功能。
3. 所有代码、规则、函数必须适配明道云平台环境、原生接口与环境变量规则。

## 三、执行任务（按顺序逐项完成）
1. **架构分层解耦**
将插件的数据取数/业务逻辑层与前端时间看板展示层彻底隔离，两层独立运行、互不耦合，便于后续分开维护与迭代。

2. **输出统一开发规范**
汇总过往实操经验与各类问题，整理完整标准文档，包含：字段解析规则、数据取值方法、查询入参格式、筛选条件编写规范。补齐现有Skill缺失内容，明确各项规则边界，杜绝盲目猜测、重复调试。

3. **封装通用标准函数**
依据梳理完成的开发规范，编写可全局复用的工具函数，覆盖字段解析、数据提取、筛选条件处理场景，实现方法直接调用，省去重复分析逻辑的工作。

4. **代码重构与模块组装**
严格遵循项目原有编码风格、代码结构与历史开发经验，结合解耦方案、标准工具函数完成代码重构与模块整合。仅基于我提供的旧版预约看板代码做改造，不引入外部无关代码。

5. **环境适配、异常兜底与功能测试**
针对明道云平台做环境适配优化，完善异常兜底逻辑，修复现存数据错误；完成全场景回归测试，保证插件整体运行稳定。

## 四、具体要求
1. 在工作目录下重新写
2. 工作目录为D:\AI\项目源码\明道云插件开发\预约看板\预约看板新版（视图展示解耦）
3. 参考文件目录为：D:\AI\项目源码\明道云插件开发\预约看板\预约看板新版（视图展示解耦）\参考，D:\AI\项目源码\明道云插件开发\预约看板\正式版beta2（新版），D:\AI\项目源码\明道云插件开发\devtest
4. 明道云全skill文件在：[text](D:\AI\skills\.claude\skills\README.md)
## 五、请你回复确认以下内容
1. 明确说明是否完整理解全部需求、代码约束、目录读写权限规则。
2. 列出完成本次工作需要我提供哪些文件、代码片段、配置、Skill文档。
3. 说明你将如何保障原有代码、功能、UI、交互不被改动。
4. 说明你将如何严格遵守目录权限，保证不操作授权目录以外的任何文件。明道云预约看板视图插件 开发需求 Prompt

