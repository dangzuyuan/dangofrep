# 审查报告 — 预约时间看板架构解耦重构

**版本**: v1.0.0  
**日期**: 2026-05-30  
**审查对象**: story/设计文档.md v1.0.0  
**审查级别**: very thorough

---

## 1. 问题清单

| # | 严重度 | 文件:行号 | 问题描述 | 根因 | 修复建议 |
|---|--------|-----------|----------|------|----------|
| 1 | **高** | `参考/timecalendar/timeline/hooks/useDrag.js:239-248` | TimeCalendar回调数据格式与设计文档/组件规则文档不一致。useDragMove的`onEventDrop`实际发送`{type:'MOVE', eventId, oldResourceId, newResourceId, newStart, newEnd}`，但设计文档5.3定义为`{eventId, newResourceId, newStart, newEnd}`（无`type`和`oldResourceId`字段）。useDragResize/useDragCreate同理 | TimeCalendar组件内部实现添加了额外字段，文档未同步 | 更新设计文档5.3的回调签名，标注额外字段；确认dragHandler.js解构兼容性 |
| 2 | **中** | `正式版beta2/src/utils/adapters.js:42-44` | adaptEvents输出时间格式不一致。`ev.start`为"HH:mm"时不补秒，但通过dur计算的endTime为"HH:mm:ss"格式 | adaptEvents对不同分支的秒数处理不统一 | 为startTime统一补":00"后缀，确保所有输出均为"HH:mm:ss" |
| 3 | **中** | `正式版beta2/src/utils/dragHandler.js:111` | `onEventDelete`已定义但未接入TimeCalendar，App.jsx也未传递 | dragHandler预留了删除功能但TimeCalendar组件未暴露删除回调 | 确认是否需要删除功能，如需要则添加`onEventDelete` prop |
| 4 | **中** | `正式版beta2/src/utils/helpers.js:4-8` | `createLogger`函数无迁移目标。设计文档重复分析遗漏此函数 | 设计文档重复分析不完整 | 在设计文档2.2补充`createLogger`条目，标记为"废弃" |
| 5 | **中** | 设计文档 | 遗漏`src/utils.js`(254行)的代码重复分析。其中`extractTime`、`safeParseJson`等与parseField.js重叠 | 审查只覆盖utils/子目录，遗漏了根目录utils.js | 在设计文档2.2补充src/utils.js条目 |
| 6 | **中** | `正式版beta2/src/hooks/useDataLoader.js:45` | `mainEventHeaderField`参数解析在useDataLoader中，但设计文档5.4未显式覆盖。遗漏mainEndParam、mainFontParam | 设计文档参数枚举不完整 | 补充mainEventEndTime、mainEventHeaderField、mainEventFontColor的处理说明 |
| 7 | **低** | 设计文档3.1 | TimeCalendar UI层的Toolbar.jsx与业务层src/components/Toolbar.jsx区分不明确 | 两个Toolbar同名不同职责 | 明确区分：timeline/Toolbar是组件内部工具，components/Toolbar是业务工具栏 |
| 8 | **低** | 设计文档5.4 vs STANDARD.md | STANDARD.md第7节`resolveWorksheet`重复出现在"参数解析"和"数据获取"两组 | STANDARD.md排版疏漏 | 从"数据获取"分组移除重复条目 |
| 9 | **低** | `参考/timecalendar/timeline/hooks/useDrag.js:527-530` | useDragResize状态清理缺少RAF守卫，与正常cleanup流程不一致 | 异常清理路径与正常路径不统一 | 统一setState方式 |
| 10 | **低** | constants.js + parseField.js + helpers.js | COLOR_PALETTE/CP三处定义。设计文档待确认#8已识别但2.2表未列全 | parseField.js自含常量减少依赖但导致重复 | parseField.js从constants.js导入COLOR_PALETTE |

---

## 2. 设计偏离清单

| # | 偏离项 | 设计文档描述 | 实际代码 | 影响 |
|---|--------|-------------|----------|------|
| 1 | onEventDrop回调签名 | `{eventId, newResourceId, newStart, newEnd}` | 实际多`type:'MOVE'`和`oldResourceId` | 低：JS解构兼容 |
| 2 | onEventResize回调签名 | `{eventId, newStart, newEnd}` | 实际多`type:'RESIZE'` | 低 |
| 3 | onSelectSlot回调签名 | `{resourceId, start, end}` | 实际多`type:'CREATE'` | 低 |
| 4 | adaptEvents输出格式 | 未明确指定"HH:mm:ss" | startTime可能"HH:mm"也可能"HH:mm:ss" | 中 |
| 5 | onEventDelete | Props中无此prop | dragHandler.js已实现但未布线 | 中：功能存在但无法触发 |
| 6 | createLogger | 2.2声称所有函数已列出 | 遗漏helpers.js:4-8 | 低 |
| 7 | resolveWorksheet文档重复 | 归入parseParam.js | STANDARD.md两处出现 | 极低 |
| 8 | App.jsx参数解析列表 | 列出5个param | 实际useDataLoader解析了9个main事件参数 | 低 |

---

## 3. 遗漏清单

### 3.1 Beta2 未提及文件

| # | 文件 | 说明 | 建议 |
|---|------|------|------|
| 1 | `src/utils.js` (254行) | 旧版统一工具函数，extractTime等与parseField.js重复 | 废弃，列入清理清单 |
| 2 | `src/operateLoader.js` (247行) | 旧版预约加载器，已被mainEventLoader替代 | 废弃 |
| 3 | `src/scheduleLoader.js` | 旧版排班加载器 | 废弃 |
| 4 | `src/LogPanel.jsx` | 旧版日志面板，已被DebugPanel覆盖 | 废弃 |
| 5 | `src/EventBar.jsx` | beta2根目录旧版，与TimeCalendar内EventBar同名不同 | 废弃 |
| 6 | `src/TableHeader.jsx` | beta2根目录旧版 | 废弃 |
| 7 | `src/TimelineAxis.jsx` | beta2根目录旧版 | 废弃 |
| 8 | `src/drag/` 目录 | 旧版拖拽模块，已被useDrag.js+dragHandler.js替代 | 废弃 |
| 9 | `src/hooks/useDrag.js` (49行) | 旧版拖拽Hook | 废弃 |
| 10 | `src/components/StaffColumn.jsx` | beta2旧版组件 | 废弃 |
| 11 | `src/components/TableHeader.jsx` | beta2旧版组件 | 废弃 |

### 3.2 设计文档内容遗漏

| # | 遗漏项 | 说明 |
|---|--------|------|
| 1 | `createLogger`函数处理方案 | helpers.js:4-8，需明确废弃 |
| 2 | `mainEventEndTime`解析说明 | useDataLoader.js:43解析但设计文档未列 |
| 3 | `mainEventFontColor`解析说明 | useDataLoader.js:48解析但设计文档未列 |
| 4 | `mainEventHeaderField`主体匹配优先级 | 5.4工具函数表未说明该参数 |
| 5 | `src/utils.js`中重复函数清单 | 旧版文件未列入重复分析 |
| 6 | ConfigPanel/TestDataModal迁移决策 | 列为UI层但未说明生产环境策略 |

---

## 4. 审查结论

设计文档与参考代码的**整体一致性良好**。核心架构分层、模块职责、函数签名、数据格式均正确对应。

**优先修复**:
1. [高] 更新设计文档中TimeCalendar回调签名（补充type/oldResourceId字段）
2. [中] 统一adaptEvents输出时间格式为"HH:mm:ss"
3. [中] 补充设计文档遗漏的参数解析说明（mainEnd/mainHeader/mainFont）
4. [中] 补充beta2过期文件清理清单

**次要处理**:
5. [低] 确认onEventDelete是否需要接入
6. [低] 统一COLOR_PALETTE/CP色板来源
7. [低] 区分两个Toolbar组件职责
